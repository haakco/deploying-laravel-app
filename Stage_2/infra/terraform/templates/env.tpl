

DB_CONNECTION=pgsql
DB_HOST=${db-host}
DB_PORT=${db-port}
DB_DATABASE=${db-database}
DB_USERNAME=${db-user}
DB_PASSWORD=${db-password}

REDIS_HOST=${db-redis-host}
REDIS_PASSWORD=${db-redis-port}
REDIS_PORT=${db-redis-password}
